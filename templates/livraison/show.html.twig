{% extends 'base.html.twig' %}

{% block title %}Livraison{% endblock %}

{% block body %}
    <h1>Livraison</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ livraison.id }}</td>
            </tr>
            <tr>
                <th>Date de Livraison</th>
                <td>{{ livraison.dateLiv ? livraison.dateLiv|date('Y-m-d') : '' }}</td>
            </tr>
             <tr>
            <th>Paiement</th>
            <td>{{ livraison.factures.paiment }}</td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('livraison_index') }}" class="btn btn-warning">Revenir à la liste des livraisons</a><br>

    <a href="{{ path('livraison_edit', {'id': livraison.id}) }}" class="btn btn-info">Modifier</a>

    {{ include('livraison/_delete_form.html.twig') }}
{% endblock %}
