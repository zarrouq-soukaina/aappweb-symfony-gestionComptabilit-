{% extends 'base.html.twig' %}

{% block title %}Client{% endblock %}

{% block body %}
    <h1>Client</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ client.Nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ client.Prenom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ client.Adresse }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ client.Type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('client_index') }}"  class="btn btn-warning" >Revenir à la liste des clients</a><br>

    <a href="{{ path('client_edit', {'id': client.id}) }}"  class="btn btn-info">Modifier</a>

    {{ include('client/_delete_form.html.twig') }}
{% endblock %}
