{% extends 'base.html.twig' %}

{% block title %}Fournisseur{% endblock %}

{% block body %}
    <h1>Fournisseur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fournisseur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ fournisseur.Nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ fournisseur.Prenom }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ fournisseur.Type }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ fournisseur.adresse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fournisseur_index') }}" class="btn btn-warning">Revenir à la liste des fournisseurs</a>

    <a href="{{ path('fournisseur_edit', {'id': fournisseur.id}) }}" class="btn btn-info">Modifier</a>

    {{ include('fournisseur/_delete_form.html.twig') }}
{% endblock %}
