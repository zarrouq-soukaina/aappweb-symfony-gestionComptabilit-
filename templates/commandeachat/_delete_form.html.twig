<form method="post" action="{{ path('commandeachat_delete', {'id': commandeachat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commandeachat.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
