{% extends 'base.html.twig' %}

{% block title %}Facture{% endblock %}

{% block body %}
    <h1>Facture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ facture.id }}</td>
            </tr>
            <tr>
                <th>DateFact</th>
                <td>{{ facture.dateFact ? facture.dateFact|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Paiment</th>
                <td>{{ facture.paiment }}</td> 
            </tr>
            
             
    
        </tbody>
    </table>

    <a href="{{ path('facture_index') }}" class="btn btn-warning">Revenir à la liste des factures</a><br>

    <a href="{{ path('facture_edit', {'id': facture.id}) }}" class="btn btn-info">Modifier</a>

    {{ include('facture/_delete_form.html.twig') }}
{% endblock %}
